---
title: "Twilio SIP Trunking"
icon: "phone-volume"
description: "Connect your Twilio Elastic SIP Trunk to Konnect-U for inbound and outbound AI calls"
---

# Twilio SIP Trunking Setup

This guide walks you through connecting your Twilio Elastic SIP Trunk to Konnect-U so your AI assistant can make and receive calls using your existing Twilio phone numbers.

## Prerequisites

Before you begin, make sure you have:

- A Twilio account with **Elastic SIP Trunking** enabled
- At least one Twilio phone number
- Access to the Konnect-U dashboard

## Step 1: Create and Configure Your SIP Trunk in Twilio

### Create the Trunk

1. In your Twilio Console, go to **Elastic SIP Trunking**
2. Click **Create new SIP Trunk**
3. Give your trunk a name (e.g., `Konnect-U Trunk`)
4. Click **Create**

### Configure Termination (for outbound calls)

5. In the left sidebar, click **Termination**
6. Set the **Termination SIP URI** to a unique domain like:

   ```
   yourcompany.pstn.twilio.com
   ```

   <Frame>
     <img
       src="/resources/twillio-new-access-control.jpeg"
       alt="Twilio New Access Control"
     />
   </Frame>

   <Frame>
     <img
       src="/resources/twillio-new-access-control2.jpeg"
       alt="Twilio New Access Control"
     />
   </Frame>

### Configure Origination (for inbound calls)

7. In the left sidebar, click **Origination**
8. Click **Add new Origination URI** and enter the SIP server address:

   ```
   sip:5wed79aeavr.sip.livekit.cloud
   ```

   <Frame>
     <img
       src="/resources/twillio-setup-origination1.jpeg"
       alt="Setup Origination"
     />
   </Frame>

## Step 2: Assign Numbers to Your SIP Trunk

Now assign your Twilio phone numbers to the trunk you just created.

1. In your SIP trunk, click **Numbers** in the left sidebar
2. Click **Add a Number** and select the phone number(s) you want to use

   <Frame>
     <img
       src="/resources/twillio-move-numbers1.jpeg"
       alt="Move numbers to Elastic SIP Trunking"
     />
   </Frame>

<Tip>
For future numbers, you only need to repeat this step and Step 3 below. The trunk configuration from Step 1 is already done.
</Tip>

## Step 3: Import Numbers to the Platform

Now import the number so the platform knows how to route calls.

1. Go directly to the Phone Numbers page in your dashboard:

   **[https://app.konnect-u.com/phone-numbers](https://app.konnect-u.com/phone-numbers)**

   If it takes you to the login page, log in first — it will automatically redirect you to the Phone Numbers page.

2. Click **"Integrate SIP trunk"** (top-right corner of the page)

   <Frame>
     <img
       src="/resources/twillio-import-autocalls-sip1.png"
       alt="Import numbers via SIP trunk"
     />
   </Frame>

3. Fill in your phone number and SIP credentials:

   ```
   Phone number: e.g., +1 555 0123
   Username: Your extension username
   Password: Your extension password
   ```

   <Frame>
     <img
       src="/resources/twillio-autocalls-auth-integration.png"
       alt="Fill in your details based on your integration type"
     />
   </Frame>

4. Enter the **Termination SIP URI** you created in Step 1:

   ```
   Server address: e.g., yourcompany.pstn.twilio.com
   ```

   <Frame>
     <img
       src="/resources/twillio-outbound-settings.PNG"
       alt="Twilio Connect Number via SIP"
     />
   </Frame>

5. Select the **Authorization type**. If you configured IP-based authentication in Step 1, select IP and add these addresses:

   ```
   54.244.51.0
   54.244.51.1
   54.244.51.2
   54.244.51.3
   54.172.60.0
   54.172.60.1
   54.172.60.2
   54.172.60.3
   ```

   <Frame>
     <img
       src="/resources/twillio-autocalls-credentials-ip-type1.png"
       alt="Twilio IP authorization"
     />
   </Frame>
